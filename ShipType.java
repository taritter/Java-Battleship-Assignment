/**this is the shipType enum class that contains the names for each ship so it then can be called in fleet
*/
public enum ShipType 
{
   ST_AIRCRAFT_CARRIER,
   ST_BATTLESHIP,
   ST_CRUISER,
   ST_SUB,
   ST_DESTROYER
}